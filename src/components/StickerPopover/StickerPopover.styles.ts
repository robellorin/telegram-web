import { CSSProperties, createStyles } from '@material-ui/styles';

const stickersWrapper: CSSProperties = {
  width: 340,
  height: 400,
  pointerEvents: 'all',
  overflow: 'auto'
};

export const styles = createStyles({
  stickersWrapper
});
